# ==================== 前端 Vue 专属忽略 ====================
# 依赖包（体积大，无需上传，npm install 可重新安装）
frontend/node_modules/
# 打包构建产物（部署时用，源码仓库无需存储）
frontend/dist/
frontend/build/
frontend/.output/  # Vue3 + Vite 项目的打包目录
# 环境变量文件（含敏感信息：如接口地址、密钥、token 等）
frontend/.env
frontend/.env.local
frontend/.env.development.local
frontend/.env.production.local
frontend/.env.test.local
# IDE 配置文件（个人开发环境，无需共享）
frontend/.idea/
frontend/.vscode/
frontend/.project
frontend/.classpath
# 缓存与日志文件
frontend/.npm/
frontend/.pnpm-store/
frontend/npm-debug.log*
frontend/yarn-debug.log*
frontend/yarn-error.log*
frontend/pnpm-debug.log*
# 编辑器缓存
frontend/*.suo
frontend/*.ntvs*
frontend/*.njsproj
frontend/*.sln
frontend/*.sw?
# Vue CLI 临时文件
frontend/.eslintcache
frontend/node_modules/.cache/
# 单元测试/端到端测试产物
frontend/coverage/
frontend/test-results/
# 其他临时文件
frontend/*.log
frontend/*.tmp
frontend/*.bak

# ==================== 后端 Go 专属忽略 ====================
# 编译产物（Go 编译后的二进制可执行文件，跨平台不同，无需上传）
backend/*_linux
backend/*_windows.exe
backend/*_darwin
backend/[你的项目名]  # 替换为你的 Go 项目二进制文件名（如 backend/file_trans）
backend/[你的项目名].exe
# 依赖缓存（Go mod 管理时，vendor 目录可忽略，go.mod/go.sum 已记录依赖）
backend/vendor/
# 环境变量与配置文件（含敏感信息：数据库账号、密钥、端口等）
backend/.env
backend/.env.*
backend/config.yaml
backend/config.json
backend/config.ini
# IDE 配置文件（Goland/VS Code 等，个人环境）
backend/.idea/
backend/.vscode/
backend/*.iml
backend/*.ipr
backend/*.iws
# 日志文件（运行时生成，无需上传）
backend/logs/
backend/*.log
# 调试与临时文件
backend/*.debug
backend/*.tmp
backend/*.bak
backend/.DS_Store
# Go 模块缓存（本地缓存，无需上传）
backend/go.sum.bak
backend/.go/pkg/
# 其他编译残留
backend/*.o
backend/*.a
backend/*.so

# ==================== 通用忽略（全项目适用） ====================
# 系统文件
.DS_Store  # Mac 系统
Thumbs.db  # Windows 缩略图
# 通用日志与临时文件
*.log
*.tmp
*.bak
*.swp
# 压缩包
*.zip
*.tar.gz
*.rar
# IDE 全局配置（非项目专属）
.idea/
.vscode/
# 敏感文件（避免泄露账号密码、密钥等）
*.pem
*.key
*.crt
.env
# 其他无用文件
.DS_Store

